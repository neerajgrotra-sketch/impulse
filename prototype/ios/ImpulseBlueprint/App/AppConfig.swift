import Foundation

/// Milestone 1 config — hardcoded on purpose. No settings screen, no environment
/// switching. Fill these in from the Supabase project dashboard (Project Settings → API).
enum AppConfig {
    static let supabaseURL = "https://YOUR-PROJECT-REF.supabase.co"
    static let supabaseAnonKey = "YOUR-SUPABASE-ANON-KEY"
}

/**
 * Ported verbatim from ThinkingView.swift. Describes *what kind* of
 * understanding is forming — never a percentage, never a completed step,
 * never a claim about internal reasoning. Order matters (values → themes →
 * strengths → friction → decision-making → the Blueprint itself) but the
 * cycle has no fixed length or endpoint; see ThinkingScreen.tsx.
 */
export const thinkingPhrases = [
  "Understanding your values",
  "Looking for recurring themes",
  "Identifying your strengths",
  "Finding friction points",
  "Understanding how you make decisions",
  "Building your Human Blueprint",
] as const;
